/*
 * Restraint.h
 *
 *  Created on: Sep 5, 2013
 *      Author: e4k2
 */

#ifndef RESTRAINT_H_
#define RESTRAINT_H_

#include <list>

using namespace::std;

/**
 * Represents a proton-proton distance restraint
 * The direction does not matter; i.e. contact is considered undirected
 */
class Restraint
{
//	list<PCAssignment*> assignments; // list of assignments that directly supports this restraint
//	double ubDist; // upper bound distance derived from peak intensities
//	Restraint();
//	Restraint(PCAssignment* pca);
//	Restraint(const Restraint& r);
//	friend void swap(Restraint& first, Restraint& second);
//	Restraint& operator=(Restraint r);
//	void print() const;
//	virtual ~Restraint();
//	bool operator==(const Restraint& r) const;
};

#endif /* RESTRAINT_H_ */
